version: '3.8'

services:
  filabridge:
    image: ghcr.io/donwmason/filabridge:latest
    ports:
      - "7456:7456"
    environment:
      - FILABRIDGE_DB_PATH=/app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped